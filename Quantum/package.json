{
  "name": "quantum-studio-v3-static",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo No build step needed for pure static site",
    // "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
